<view class="container" bind:tap="onBookTap">
    <image src="{{image}}" class="img"/>
    <view class="content">
        <text class="title">{{title}}</text>
        <text class="author">{{author}}</text>
        <slot/>
    </view>
</view>